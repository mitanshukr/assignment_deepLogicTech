Please run `node project.js` in the terminal to start the server.

### Endpoints:
`/` : homepage html<br>
`/getTimeStories` : get Stories in JSON.<br>
`/anythingElse` : 404 Not Found.

<br>
Thanks!
